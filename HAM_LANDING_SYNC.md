# 이키가이 햄찌 랜딩 복사 — 2026-09-17

## 요청과 확정 범위

- 기존 이키가이 랜딩 페이지를 `/ham` 슬러그로 복사한다.
- `/ham` 신청을 동일한 신청 스프레드시트의 `10월 이키 재능 햄찌` 탭에 저장한다. 최초 이름 `10월 이키 재능`에서 후속 요청으로 변경한다.
- `/ham` 첫 화면의 고양이를 사용자 첨부 이미지의 햄스터로 교체한다.
- 기존 `/`, `/uni`, `/job`, `/worker`의 고양이, 문구, 신청 항목, `Ikigai_Landing` 저장 대상(실제 표시명 `이키가이 기존`)은 유지한다.

## 검토와 기획

- 사용자 심리: 기존 신청 흐름을 유지하면서 햄스터 캐릭터로 친근한 첫인상을 준다. MBTI별 행동 차이나 전환율 효과는 검증되지 않았으므로 가정하지 않는다.
- 데이터 확인: 현재 라우트 설정, 신청 payload, 캐릭터 크기와 하단 배치를 확인했다. 첨부 이미지의 문구는 페이지 문구 변경 지시로 취급하지 않는다.
- 비주얼: 기존 모바일 222px / PC 264px 너비와 비율 유지, 도표 아래 우측 배치, 다음 검정 섹션과 0px 간격을 유지한다.
- 구현: 별도 페이지를 중복 관리하지 않고 경로별 설정으로 기존 컴포넌트를 재사용한다.

## 프런트엔드 변경

- `vercel.json`: `/ham` 요청을 `/index.html`로 연결한다.
- `src/IkigaiLanding.jsx`: `/ham`에 햄스터 이미지, `variant: '이키가이 가이드북 랜딩 / 햄찌'`, `sheetTab: 'Ikigai_Ham'`을 지정한다. 최초 배포에서는 표시명 `10월 이키 재능`을 직접 전송했으며, 이름 변경부터 안정적인 논리 ID를 사용한다.
- 기존 경로의 신청은 `sheetTab: 'Ikigai_Landing'` 기본값을 유지한다.
- 신규 캐릭터 자산 경로: `src/assets/ikigai-study-hamster.png`.

## 햄스터 이미지 출처와 편집

- 사용자 첨부 원본: `codex-clipboard-384bd75e-3045-48a3-befd-9e4f8ff849a6.jpg`.
- 내장 `image_gen` 편집 도구로 햄스터만 투명 배경 PNG로 추출했다. 원본 표정, 고개 각도, 턱에 댄 손, 털 색상, 녹색 점퍼와 크림색 소매를 유지하고 흰 배경·한글 문구·낙서만 제거하도록 요청했다.
- 프롬프트 요약: 기존 캐릭터 정체성·포즈·세부 묘사를 보존하고 새 물체나 그림자를 추가하지 않으며, 귀와 털을 자르지 않고 하단 몸통을 캔버스 바닥에 맞춘 투명 PNG를 만든다.
- 결과: 1242 × 1266 RGBA PNG, 약 1.73 MB. 자산 생성 에이전트가 배경 투명도, 문구 제거, 캐릭터 보존, 하단 배치를 확인했다.

## 신청 시트 최초 운영 반영

- 기존 신청 스프레드시트에 `10월 이키 재능` 탭을 생성했다. 탭 ID는 `1408861281`이며, 기존 이키가이 항목 14개에 `캠페인 월`을 더한 15개 헤더를 사용한다.
- 신규 `IkigaiHam.gs` 모듈과 기존 `Code.gs`의 등록·읽기 전용 준비 점검·Telegram 이키가이 분류 3곳을 반영했다. 저장 후 편집기의 코드를 다시 읽어 의도한 내용과 일치함을 확인했다.
- 기존 Apps Script 웹 앱의 동일 배포를 **버전 47**, **2026-09-17 01:09 KST**로 업데이트했다. 웹훅 주소와 기존 신청 경로는 유지했다.
- 배포된 웹훅에 준비 점검 요청을 보내 `ok: true`, `ready: true`, `sheetId: 'Ikigai_Ham'`, `sheet: '10월 이키 재능'`, `schemaVersion: 'ikigai-ham-v1'` 응답을 확인했다.
- Google Sheets 커넥터로 대상 탭 `A1:O2`를 읽어 헤더 15개와 신청 데이터가 없는 상태를 확인했다. 확인 과정에서 실제 신청 행이나 외부 메시지를 생성하지 않았다.
- Google Sheets 브라우저에서도 녹색 헤더, 헤더 행 고정, 빈 신청 데이터 영역을 시각 확인했다.

## 검증 상태

- 프런트엔드 경로 7개(`/`, `/uni`, `/job`, `/worker`, `/ham`, `/ham/`, `/ikigai-landing/`)에 대해 실제 코드의 라우트 설정과 payload를 로컬에서 검증했다. `/ham` 계열만 `10월 이키 재능`/햄찌로 분기하며 기존 경로는 `Ikigai_Landing`/고양이로 유지된다. 네트워크 요청이나 실제 신청 데이터는 생성하지 않았다.
- `/ham` Vercel rewrite와 기존 모바일/PC 캐릭터 너비 유지 검증, `git diff --check`, `VERCEL=1 npm run build`를 통과했다. 기존 대용량 JS 번들 경고는 남아 있다.
- 실제 운영 `Code.gs` 백업을 불러오는 Node VM 테스트 6/6을 통과했다. 탭 준비의 재실행 안전성, 읽기 전용 점검과 누락·중복 헤더 거절, 한글/영문 대상 이름 라우팅, 원본 전체 필드 매핑, 기존 탭 격리, 알림 분류를 검증했다. 테스트는 모의 시트에서 수행했으며 외부 요청을 보내지 않았다.
- 로컬 브라우저 390px 모바일 / 1280px PC에서 가로 넘침 없음, 햄스터 하단과 검정 섹션 사이 간격 0px, CTA의 신청 영역 이동을 확인했다.
- 실제 탭 생성과 Apps Script 운영 배포 및 준비 점검은 완료했다. 실제 신청 제출 검증은 수행하지 않았으며 기존 `no-cors` 요청의 완료 표시는 실제 시트 저장 성공을 보장하지 않는다.
- 프런트엔드 커밋 `66a5880`의 Vercel 배포가 성공했다. 공개 주소 `https://ikigai-landing-three.vercel.app/ham`에서 페이지, 신청 링크, 햄스터 PNG의 정상 로드(1242 × 1266)를 확인했다.
- 작업 이전부터 수정되어 있던 `SYNC.md`와 워크북/학생 캐릭터 자료는 이 변경에 포함하지 않는다.

## 후속 탭 이름 변경 — 2026-09-17

- 요청: 햄찌 신청 탭 이름을 `10월 이키 재능`에서 `10월 이키 재능 햄찌`로 변경한다.
- 프런트엔드: `/ham`의 전송 대상만 표시명 문자열에서 고정 논리 ID `Ikigai_Ham`으로 변경했다. 경로, 이미지, 신청 항목, 웹훅 주소와 `no-cors` 처리에는 변경이 없다.
- 기존 경로 확인: 운영 `Code.original.gs` 및 `Code.browser-original.gs` 백업의 `SHEET_TAB_NAMES`에서 `Ikigai_Landing → 이키가이 기존` 매핑을 확인했다. `/`을 포함한 기존 경로는 `Ikigai_Landing`을 전송하므로 이 탭에 계속 연결된다.
- 로컬 검증: 경로 7개의 payload를 실제 코드로 평가해 `/ham` 계열은 `Ikigai_Ham`, 기존 경로는 `Ikigai_Landing`을 전송함을 확인했다. 기존 커밋과 전체 프런트엔드 소스를 비교해 `/ham`의 `sheetTab` 값 외 코드 변경이 없음을 검증했으며, 프로덕션 빌드와 diff 검사를 통과했다. 실제 신청은 전송하지 않았다.
- 운영 반영: 기존 탭 ID `1408861281`의 표시명을 `10월 이키 재능 햄찌`로 변경했다. 모듈의 표시명 매핑과 준비 점검 대상을 갱신하고 구명칭 alias를 추가해 같은 웹 앱 버전 **48(2026-09-17 01:21 KST)**로 배포했다. 별도 탭이나 신청 데이터는 생성하지 않았다.
- 완료 확인: 영문 ID와 구명칭으로 각각 읽기 전용 준비 점검을 호출해 새 이름의 동일 탭에 `ok: true`, `ready: true`를 확인했다. Google Sheets 메타데이터와 브라우저 화면에서도 동일 탭 ID 및 새 이름을 확인했다. 운영 원본과 패치 코드로 수행한 로컬 테스트는 각각 **8/8 통과**했다.
